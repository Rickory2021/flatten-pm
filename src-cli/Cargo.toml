# crates/src.cli/Cargo.toml
[package]
name = "flatten-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.6", features = ["derive"] }
dirs = "7.0.0"
flatten-core = { path = "../crates/flatten-core" }
rusqlite = { version = "0.40.2", features = ["bundled"] }
serde = "1.0.229"
serde_json = "1.0.151"
